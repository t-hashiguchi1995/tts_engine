# deploy/

Cloud Run のデプロイは **Terraform** に移行しました。

- 設定: [`../terraform/`](../terraform/)
- 手順: [`../terraform/README.md`](../terraform/README.md)

以前の `cloudrun-*.yaml` と `scripts/deploy.sh` は削除済みです。
